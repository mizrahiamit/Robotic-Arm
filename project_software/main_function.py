import sys
import cv2
import numpy as np

  
def check_coordinates(dest_coor,arm_src_coor):
    #return true if move is possible, else false.
    if (dest_coor[1] < 640) :
    print "Checking if move is possible"
    return  true

def disable_arm(self):
    #disabling pwm port, to the servo motors.
    print "Disabling arm"
    return true  